import { builtin }

pub component Println<T> (data T) (sig T) {
    nodes {
      // implicitly refer to std/builtin package to avoid recursion
      builtin.Println<T>
    }
    net {
        :data -> println:data
        println:sig -> :sig
    }
}
